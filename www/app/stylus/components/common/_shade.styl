.shade {
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, .4);
    position: fixed;
    top: 0;
    left: 0;
    z-index: 1000;
    
    opacity: 0;
    pointer-events: none;
    transition: .5s all;

    &.is-on {
        opacity: 1;
        pointer-events: auto;
    }
}