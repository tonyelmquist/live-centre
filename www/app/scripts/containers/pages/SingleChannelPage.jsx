import React from 'react';

export default function SingleChannelPage() {
    return (
      <div className="slide">
        <h1>One channel to rule them all</h1>
        <p>One channel to find them, one to bring them all, and in darknes bind them</p>
      </div>
    );
}
