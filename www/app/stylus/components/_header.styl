.top-menu{
	display:block
}

.header{
  width: 100%;
  background: linear-gradient(0deg, #f5f5f5, #fff);
  text-transform: none !important;
  position: fixed;
  top: 0;
  z-Index: 999;
  color: #000;
}

.logo *{
    color: $color_ocean;
    font-size: $font_xl !important
    font-weight: bold !important;
      
}
.rightMenu{
  float:right;
}

@media (min-width: $big_screen){
  .rightMenu{
    padding-right:20px;
  }
}


#header-container *{
  text-transform: none !important;
}
/*
.menuItem{
  margin: 6px !important;
	display:inline-block !important;
  text-transform: lowercase !important;
  color: #000 !important;
}*/


//Font size of everything in header.
#header-container button *, #header-container p{
}

@media (min-width: $big_screen){
  .menuItem{
    //margin: 20px 10px 25px 20px !important;
    margin: 5px !important;
  }
}

.menuItem .menuItemLabel{
  padding:12px 0;
  display:inline-block;
}

.fullscreen_menu{
	background-color: grey;
	height: 100vh;
}
.fullscreen_menu .menuItem{
	display: block;
	width: 100%;
}

.fade-enter {
  opacity: 0.01;
}

.fade-enter.fade-enter-active {
  opacity: 1;
  transition: opacity 500ms ease-in;
}

.fade-leave {
  opacity: 1;
}

.fade-leave.fade-leave-active {
  opacity: 0.01;
  transition: opacity 300ms ease-in;
}

.expandableMenu{
  text-align:center;
  background-color: $color_ocean;
  position: fixed;
  bottom: $bottom_offset;
  width: 100%;
  right: 0;
  z-Index: 999999;
  overflow:auto;
  color: #fff;
  list-style-type: none;
}

//All paragraphs(items) in menu
.expandableMenu p{
  color: #efefef;
  padding: 13px 0 13px 0;
  margin: 0;
  opacity: 1;
  text-align:center;
}
.closedMenu{
  display:none;
}

//Only the links
p.expandableMenuItem{
  color: #fff;
}
.active .expandableMenuItem {
  background-color: rgba(0, 174, 255, .2); //#00aeff
}

//Only the dividers
p.divider{
  padding: 35px 10px 10px;
  font-weight:bold;
  border-bottom:solid 1px rgba(250, 250, 250, 0.5);
  display:inline-block
  
  color: #efefef;
}

.expandableMenu a{
  color: #fff;
  text-decoration:none;
}

.closeButton{
  top: 20px;
  right: 20px
  position: absolute;
}
.bottomTabs{
  position:fixed;
  bottom: 0;
  z-index: 900;
  width: 100%;
  box-shadow: 0px 0px 21px rgba(0, 0, 0, .1);
  @media (min-aspect-ratio: 13/9) {
    z-index: 900;
  }
}

.tabIcons{
  font-size: 1.4em;
  margin-bottom: 4px;
}

// MENU BUTTON ANIMATIONS


.animMenuIcon{
  padding: 0 0 4px 0;
  vertical-align: middle;
  stroke-linecap: round;
}

.animMenuIcon .bottom, .animMenuIcon .middle, .animMenuIcon .top{
    transition: all 0.3s $cubic;
    transform-origin: center;
}

.animMenuIcon.closed.subPage{
  .top{
      transform-origin: top right;
      transform: rotate(-45deg) translate(2px, 0) scale(0.5, 1);
  }
  .bottom{
      transform-origin: bottom right;
      transform: rotate(45deg) translate(2px, 0) scale(0.5, 1);
  }
  .middle{
    transform: translate(22px, 0);
  }
}

.animMenuIcon.open{
  .top{
      transform-origin: center;
      transform: rotate(-45deg) translate(-3.5px, 3.5px);
  }
  .bottom{
      transform-origin: center;
      transform: rotate(45deg) translate(-3px, -3.5px);
  }

  .middle{
    opacity: 0.5
    transform: translate(22px, 0);
  }

}
